Simple url shortener to study Rust.
Next steps:

* **Create simple shortner without validations and using counter as keys**
* Create a variant using Hash to keys
* Add a simple cli-menu
* Do batch operations with files
* Create configurations to allow different strategies
* Add capabillity to store and retrieve info in files
* Add capabillity to remove key
* Add capabillity to do operations with REST
* Create a next.js screen
* Create a distributed variant that use consensus algoritm like paxos or raft
